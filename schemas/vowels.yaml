fields:
  - name: ID
    type: integer
  - name: LETTER
    type: string
    constraints:
      minLength: 1
      maxLength: 1
      pattern: "^[aeiou]$"
  - name: VOWEL
    type: boolean
    trueValues: [ "TRUE"]
    falseValues: [ "FALSE" ]
